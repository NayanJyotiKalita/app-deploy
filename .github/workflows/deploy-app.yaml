name: deploy-app
 
on:
  push:
    branches:
      - feature-deploy
 
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run deployment script
        run: echo "Deploying the app..."
